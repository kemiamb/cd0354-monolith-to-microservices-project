apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-feed
  labels:
    run: backend-feed
spec:
  replicas: 2
  selector:
    matchLabels:
      run: backend-feed
  template:
    metadata:
      labels:
        run: backend-feed
    spec:
      containers:
      - name: backend-feed
        image: kemiamb/udagram-api-feed:v2
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        envFrom:
          - configMapRef:
              name: env-config
          - secretRef:
             name: env-secret